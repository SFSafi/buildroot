diff --git a/arch/arm/mach-sunxi/spl_spi_sunxi.c b/arch/arm/mach-sunxi/spl_spi_sunxi.c
--- a/arch/arm/mach-sunxi/spl_spi_sunxi.c
+++ b/arch/arm/mach-sunxi/spl_spi_sunxi.c
@@ -347,9 +347,13 @@ static enum flashtype spi0_get_flash_type(void)
 			printf("SPI-NAND: Winbond W25N01G\n");
 			type = FLASHTYPE_NAND;
 		}
+		else if (!memcmp(chipid, "\xef\xae", 2)) {
+			printf("SPI-NAND: Winbond W25N01KV\n");
+			type = FLASHTYPE_NAND;
+		}
 		else if (!memcmp(chipid, "\xc2\x12", 2)) {
 			printf("SPI-NAND: Macronix MX35LF1GE4AB\n");
 			type = FLASHTYPE_NAND;
 		}
 		else if (!memcmp(chipid, "\xc2\x14", 2)) {
 			printf("SPI-NAND: Macronix MX35LF1G24AD\n");
